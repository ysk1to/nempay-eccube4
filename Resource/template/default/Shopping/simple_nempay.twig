
{% extends  'default_frame.twig' %}

{% block javascript %}

{% endblock %}

{% block main %}

    <h2 class="title">{{ title }}</h2>
    <form name="form1" id="form1" method="post" action="?" >
        {{ form_widget(form._token) }}
        <div class="dl_table" id="payment_form_body">
            <dl>
                <dt>受注番号</dt>
                <dd>
                    <div class="form-group form-inline">
                        {{ order_id }}
                    </div>
                </dd>
            </dl>
            <dl>
                <dt>お支払い金額(円)</dt>
                <dd>
                    <div class="form-group form-inline">
                        {{ payment_total }} 円
                    </div>
                </dd>
            </dl>
            <dl>
                <dt>お支払い金額(XEM)</dt>
                <dd>
                    <div class="form-group form-inline">
                        {{ payment_amount }} XEM
                    </div>
                </dd>
            </dl>
            <dl>
                <dt>参考レート(24時間平均)</dt>
                <dd>
                    <div class="form-group form-inline">
                        {{ rate }} 円／XEM
                    </div>
                </dd>
            </dl>
            

        </div>

        <p>以上の内容で間違いなければ、下記「ご注文完了ページへ」ボタンをクリックしてください。</p>
        <p>※ ご注文完了後、お支払い用のQRコードが発行されます。</p>

        <div class="row no-padding">
            <div class="btn_group col-sm-offset-4 col-sm-4">
                <p>
                    <input type="submit" class="btn btn-primary btn-block" value="ご注文完了ページへ" />
                </p>
                <p><a href="{{ path('shopping_simple_nempay_back') }}?order_id={{ order_id }}" class="btn btn-info btn-block">戻る</a></p>
            </div>
        </div>

    </form>
{% endblock %}
